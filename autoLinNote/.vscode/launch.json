{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            //"envFile": "${workspaceFolder}/.env",  // Ensure environment variables are loaded
            "env": {
                "HF_HOME": "/Users/tanoshimi/.cache/huggingface/token" // Example environment variable
            }
        }
    ]
}
